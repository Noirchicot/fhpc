# FHPC — instructions pour Claude

- **Zéro build, zéro framework, zéro dépendance runtime.** ESM natif,
  `node:test`. Dépendances de dev autorisées si épinglées et listées dans la
  section du lot qui les introduit.
- **`git push`, création de remote, tout déploiement = gestes d'Eric.** Tendre
  la commande, ne jamais l'exécuter.
- Les contrats de blocs (`contracts/*.md`) sont **ratifiés par l'architecte**
  avant merge — un contrat non ratifié ne fait pas foi.
- 🔴 **Lire [`ui/builder/NORMES.md`](ui/builder/NORMES.md) AVANT DE DESSINER OU DE TOUCHER
  UN ORGANE.** Chaque ligne y a été tranchée par Eric, à une date — jetons, boutons, voiles,
  popups, listes, vocabulaire. ⛔ **Ne pas les redécouvrir, ne pas les rediscuter dans un lot.**
  Un lot qui contredit une ligne de ce fichier est refusé.
- **Lire [`TRAPS.md`](TRAPS.md) avant de toucher** KV, tunnel, CSS, ou tout
  fichier généré — chaque ligne y est une erreur déjà facturée.
- Lois §0.8–0.9 (dépôt public, aucun propos sur des personnes, aucun contenu
  WotC ni tiers non-CC hors SRD ; aucun serveur mondial à maintenir hors
  exception à trancher par Eric ; aucune table obligée de payer un KV cloud).
- Feuille de route complète : `~/tools/fh-phb/FHPC-V2-KICKOFF.md`.
