# Lot 213 — X1, la fiche d'un objet possédé

> Ouvert le **2026-09-17** sur `main = 2111b597`, v652 en ligne. Branche `213-fiche-x1`.
> Le plan : `FH-WEB/FHPC/Plan-ecran-X1/` (X1_gen.py → X1_cotes.json → X1_declaration.js).
> L'artefact du plan coté : https://claude.ai/artifact/LRreQXds7BEa22zqg1t9bC

---

## Mes erreurs d'abord

1. **J'ai copié la table générée telle quelle, et elle a fait rougir le garde 17.** La
   déclaration portait `mot: "Back"` ; or ce mot n'existe qu'UNE fois dans tout `ui/`, et
   c'est la coquille qui l'écrit. Le générateur émet maintenant `role: "retour"` sans le mot,
   et la porte de X1 dit **`Close`** — le mot ratifié par Eric le 07/09 pour une fenêtre qui
   ferme. ⏳ Le croquis dit « back » : **la question est rendue à Eric** (§ Ce qui reste ouvert).
2. **J'ai réécrit un garde de l'écran R, et il mesurait trois lignes au lieu du bloc entier.**
   En partageant la dalle entre `.gear` et `.x1`, le sélecteur `.gear {` a cessé d'exister ;
   le garde 4 bis de `gear-ecran.test.mjs` cherchait ce texte exact et a continué de PASSER en
   mesurant la ligne du cran serré, 365 lignes plus bas. Il épelait une implémentation. Il
   cherche maintenant le bloc par ce qu'il est, et la famille par appartenance à sa liste.
3. **La ligne du poids débordait de sa boîte.** J'y répétais la quantité (« 3 lb · ×2 · 6 lb »)
   alors que la boîte du plan est taillée sur « 3 lb · 6 lb » — 43,73 mesuré dans 44. Vu au
   banc, pas au test : un garde de source ne voit pas un texte qui déborde.
4. **L'encre du site était illisible sur le parchemin.** Premier rendu : la fiche prenait
   `--text`, qui bascule avec le thème, sur un fond opaque et clair. Elle porte maintenant son
   encre à elle, comme le bouton porte la sienne.
5. **La déchirure mangeait le premier caractère de la description.** Le parchemin était dessiné
   à la taille exacte de la dalle ; le plan dit qu'il DÉBORDE de 14 de chaque côté, pour que sa
   partie plate couvre les 375 × 500. La cote était dans le plan, pas dans le code : elle y est.

---

## Le verdict, en une phrase

**La fiche s'ouvre au tap et au clic droit sur un jeton porté, elle recouvre la dalle sans
toucher au belt, et les deux états qu'elle écrit — `attuned`, `locked` — allument enfin les deux
voyants que l'écran R dessinait depuis le lot 212 sans écrivain.**

---

## Ce que le lot livre

| fichier | ce qu'il porte |
|---|---|
| `ui/builder/x1-disposition.mjs` | la table générée, **copiée telle quelle** — 24 organes, leurs cotes, le débord du parchemin, les destinations et la liste `is` |
| `ui/builder/x1-ecran.mjs` | l'écran : la feuille des cotes construite depuis la table, et les organes |
| `ui/builder/shell.css` | l'HABIT seul — aucune position. La dalle est déclarée une fois pour les deux écrans du chapitre (`.gear, .x1`) |
| `ui/builder/tokens.css` | le parchemin, l'oxblood des mots d'état, les deux interrupteurs noirs, le cadenas et le cœur (masques SVG) |
| `ui/builder/gear-ecran.mjs` | le tap et le clic droit d'un jeton ouvrent la fiche — sur l'emplacement ET dans le collecteur |
| `ui/builder/equipment-step.mjs` | la vue `x1`, son état d'écran, le feuilletage, la copie, l'envoi, la corbeille |
| `ui/builder/shell.mjs` | le verbe `setGearFlag` — et il n'accepte que `attuned` et `locked` |
| `ui/builder/banc-x1.html` | le banc : la fiche seule, pour la REGARDER |
| `tests/x1-ecran.test.mjs` | 18 gardes, dont la loi du rang X |

---

## La loi du rang X, posée dans NORMES

📍 `cadre-rang-x-fiche-d-un-objet` · vivante · 17/09 — elle n'existait dans aucun document : elle
vivait dans trois commentaires de code, sur une parole d'Eric du 16/09 (*« les x ne s'inscrivent
pas dans le belt »*). Une règle qui ne vit que dans le fichier qui l'a écrite n'existe pas.

**Ce que le rang X décide, et rien d'autre** : il recouvre la dalle entière, le belt reste
visible et inchangé, ⛔ la 3ᵉ ligne n'est **jamais** écrite, le retour **ferme** au lieu de
reculer, et aucun état ne survit à sa fermeture. En code, la loi tient à une chose vérifiable :
`x1` n'est pas une clef de `FENETRE_DE`. Le garde 8 la tient.

---

## Ce qui a été mesuré, et comment

| mesure | résultat |
|---|---|
| `gear[N].attuned` / `.locked` contre les verbes (personnage d'exemple, `rebuild`) | **zéro violation**, zéro warning ; les deux ressortent dans `unconsumed`, comme `species.lineage`, `feat.extra` et les `location`/`boite` du pipeline |
| la largeur des trois mots d'état, police du produit, au canvas | `Equipped` **45,68** à T1/600 (déborderait des 40 de la colonne) · **36,54** à T0/600 (tient) → le cran est **T0** |
| le rendu, au navigateur, dans les DEUX thèmes | la fiche tient : parchemin opaque, encre sombre, boutons à face unique |
| le geste, dans le produit (pas au banc) | tap sur `Dagger` → la fiche s'ouvre · l'interrupteur `Attune` → `gear[0].attuned=true` au document · retour à R → le ♥ du jeton est allumé |
| le belt pendant que la fiche est ouverte | il dit toujours **« Gear »** |

`npm test` : **2284 verts, 0 rouge** (2266 avant le lot, +18 gardes neufs).

---

## Les dix corrections du soir, et ce qu'elles ont changé

Eric les a dictées en regardant le rendu. Chacune est passée par le PLAN, puis par l'écran :

| la correction | ce qu'elle a déplacé |
|---|---|
| une page = un objet ; la pagination seulement s'il y a plusieurs pages | les flèches se retirent avec elle — `hidden`, la place reste |
| quantité à gauche, nom **centré sur la page**, pagination à droite | trois ancrages au lieu d'une rangée : le nom ne bouge plus quand son voisin grandit |
| prix à 90 du bord gauche, poids à 90 du bord droit | deux ancrages, un par bord, au lieu d'un centrage commun |
| la copie en **20 × 20 dans 44**, deux pages en traits | le plus petit dessin de la fiche dans la plus grande cible |
| deux **filets** délimitent le texte, qui prend **20** de marge | la description devient un groupe de trois, collés |
| `is` n'est plus un menu : **un mot** | désarmé, il portait le voile des 20 % et disparaissait |
| plus de « destination » au-dessus du menu d'envoi | le menu est dans une phrase, la phrase le dit déjà |
| **8** entre l'envoi et les portes | 8 sépare deux groupes, 4 deux rangées d'un même groupe |
| equip et attune reprennent **l'interrupteur du menu** | le plan n'invente plus de cote : 36 × 20, pouce 16 |
| **ni livre ni `?`** | posé dans NORMES avec son argument |

⭐ **Et le parchemin a changé de loi.** Il débordait de la dalle pour que sa partie plate couvre
les organes — c'était **effacer la déchirure**, qui est le sujet. Il tient maintenant dans les
375 × 500, bords compris, et c'est le contenu qui s'en écarte : la marge de 20 du texte est la
conséquence de ce renversement, pas un goût.

**Deux défauts trouvés au banc, pas au test** : la cible de la copie mordait de 8 dans le mot de
`is` (un contrôle occupe la place de sa CIBLE, jamais de son dessin — le garde des cibles l'a
dit), et `border: 0` sur ce bouton tuait le creux (une bordure sans style vaut zéro quelle que
soit sa largeur, donc le glyphe rendait 44 au lieu de 20).

---

## Ce qui reste ouvert — et qui appartient à Eric

1. **`Close` ou `back` ?** Le croquis écrit « back ». Le dépôt réserve ce mot à la coquille
   (garde 17, loi I.5) et a ratifié `Close` pour une fenêtre qui ferme. Un mot d'Eric et la
   porte change — mais changer le mot demande de toucher au garde d'un autre lot.
2. **La corbeille détruit-elle, ou pose-t-elle au sol ?** Elle détruit, derrière un popup rouge.
   L'écran R a deux emplacements GROUND : jeter pourrait y déposer.
3. **Une pile se scinde-t-elle ?** `Send 1` d'une pile de 2 déplace aujourd'hui **toute** la
   ligne. Scinder touche la forme des données, pas l'écran : ⛔ pas inventé ici.
4. **`is` dit le genre du record** (`weapon`, `armor`, `gear`) faute d'une source tranchée : le
   rangement dans la fiche de personnage — « une attaque », « un sort » — attend ta décision.
5. **`Lock` garde son corps noir et son cadenas** pendant qu'`equip` et `attune` ont repris
   l'interrupteur du menu. Tu n'as nommé que les deux autres ; un mot et il les rejoint.
5. **`Use`** est posé et désarmé — le sous-menu par objet est un chantier à part (VTT).
6. **La déchirure du parchemin** tombe hors de la dalle, comme le plan le dit : on en voit le
   début au bord, pas la dent entière. La montrer en entier coûterait 14 de marge sur les
   quatre côtés — donc 28 de large à la description.
7. **Le plafond d'harmonisation du SRD** n'est ni appliqué ni montré : `attuned` s'écrit, rien
   ne le compte encore.

---

## Le geste de publication

⛔ Il appartient à Eric. Quand il le veut :

```
node bin/nouvelle-version.mjs && npm test
git checkout main && git merge --no-ff 213-fiche-x1
git push
```
