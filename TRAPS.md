# TRAPS — pièges déjà payés, ne pas les repayer

Copié tel quel depuis deux sources de `fh-phb` le 2026-08-07 (lot J0).
Chaque ligne est une erreur déjà facturée — en argent, en temps, ou en
production cassée. Lire avant de toucher KV, tunnel, CSS, ou tout fichier
généré.

---

## Source : `fh-phb/ARCHITECT-HANDOFF.md` §3 — « Traps already paid for »

| Trap | What happens | The rule |
|---|---|---|
| ~~`sync_from_vault.py` clobbers the hand-added toolbar in `docs/skill-builder.html` and `docs/stat-roller.html`~~ | — | **FIXED 2026-08-02** (`106782f`). The sync now injects the shell itself — `add_tool_chrome()`, idempotent, and it raises rather than guess, so a page it cannot dress keeps its published copy instead of going out bald. No more `git checkout` after a sync |
| **Patching a published copy instead of its source** — the deeper version of the trap above | the next sync silently **reverts a deployed bugfix**, and the tests still pass because they read the same reverted file | found 2026-08-02: `207f597` fixed the `/builds` revision chain in `docs/skill-builder.html`, the *published* copy, and never touched the source `~/tools/fh-skills/fh-skill-builder.html`. Running the sync would have undone a production fix, quietly. **A published page under `docs/` that has a source outside the repo is a generated artefact — fix the source, then sync.** The source is now correct; proof is that a sync leaves the working tree clean. ~~`~/tools/fh-skills` **has no git**, so back it up before editing it~~ **FIXED 2026-08-04** — git since `532516a`, private GitHub remote; `captures/` (recorded DDB traffic) deliberately untracked |
| A flex item whose `overflow` is not `visible` gets an automatic minimum size of **0**, not `min-content` | the belt measured **1px** tall in a short window and vanished | `flex:none` on dock chrome; floor the **zone**, never the scroller inside it (a scroller taller than its box overflows the row below) |
| **`facteurZoom(n)` ne peut pas convertir `n` lui-même** | `facteurZoom(n)` vaut `rect(n) / offset(n)` : écrire `rect(n) / facteurZoom(n)` se simplifie en `offset(n)`, un **entier**. Posé le 30/08 dans `centreDe` (equipment-step.mjs) sous une note qui promettait mot pour mot le contraire — *« diviser garde la fraction »*. Elle ne la gardait pas, et rien ne rougissait | mesurer le facteur sur la **racine d'échelle** (`.app`, `facteurZoomCourant()`), jamais sur le nœud qu'on convertit. `keepInView` est indemne : il divise un ÉCART entre deux rectangles distincts |
| **Un nœud posé sur `<body>` échappe au zoom** | le zoom vit sur `.app`, enfant de `<body>` : les deux fantômes de glisser-déposer se peignaient à 87 px pendant que l'organe sous le doigt en faisait 261, au cran 3 — pendant le seul geste où l'œil compare les deux | monter tout nœud flottant **dans `.app`**, avec repli sur `<body>` si elle n'existe pas |
| **Un `matchMedia` de largeur est pire qu'un `@media`** | il interroge la fenêtre **brute**, donc il ment à TOUS les crans au lieu de simplement ne pas se réévaluer. `abilities-tray.mjs` en cachait un (`min-width: 768px`) qui choisissait une cote de dé, survivant du `@media` jumeau supprimé le matin même | les gardes des seuils balaient les `.mjs` autant que les `.css` (garde 5 / 5 bis) |
| A custom property cannot be defined in terms of itself | `--cd-fs:min(var(--cd-fs),N)` is a cycle and is silently discarded | derive: JS writes `--cd-fs-pref`, CSS computes `--cd-fs` from it, media queries clamp |
| `--cd-width` resolves at `:root` | writing `--cd-fs` on the dock node moved the fonts but left the width pinned | write to `root.ownerDocument.documentElement` — `ownerDocument` keeps Table mode (PiP) correct |
| Cloudflare KV allows ~1 write/sec **per key** | five players rolling at once lose feed events | one key per event + a cursor, never one key for the whole feed |
| `fh-roll/1` is a **view** model | `parts` are display strings like `"d20 (adv)"`; unparseable | machines read the separate `intent` layer (plan §11.2) |
| Codex sometimes pushes straight to `origin/main` | your local `main` is silently behind | `git fetch` and check `origin/main` before assuming you are current |
| ~~`linkedom` lives in `/tmp/fh-player-test`~~ | — | **FIXED 2026-08-01.** It is a pinned devDependency (`package.json`, linkedom 0.18.12) and every suite does a plain `require("linkedom")`. Setup is now `npm install`. Verified by moving `/tmp/fh-player-test` aside entirely: 14/14 still green |
| A seq **counter** key re-creates the 1-write/sec limit it was meant to dodge | five players rolling at once race on `feed:{CODE}:cursor` | the sequence IS the timestamp — 13-digit padded epoch ms + random tiebreaker, no shared key (plan §11.4) |
| `addHistory` is **not** where a roll settles | a natural 1 is in history *before* the player chooses, and defying turns it into a 20; an adjusted roll never passes through it at all | broadcast at `openRollState` + the `finish-sequence` branch, gated on `rollTransactionActive()` (plan §11.4b) |
| **Marking something delivered before knowing whether it was** | `broadcastEntry` set the sent flag ahead of the `POST` result, so a transient network failure lost that roll from the shared table log **permanently** — and a later unrelated successful poll cleared the offline indicator, showing the player a false all-clear | found by package 9, fixed 2026-08-03. Claim optimistically if you must, but **un-claim on failure**, and never let an unrelated success clear a failure someone else recorded. This is a *family* of bug, not one site: any optimistic flag that is never rolled back |
| `document.hidden` is true for the **main** document while Table mode runs in PiP | gating polling on it kills the feed exactly when it is being used at the table | hidden **slows** the poll to 12s, never stops it |
| ~~`~/tools/fh-worker` has no git~~ | — | **FIXED 2026-07-29** — it is a repo now (first commit `7fe6588`). The `.bak` files remain as history; new edits go through git |
| An HTTPS page **cannot** fetch `http://` | every self-hosting option that skips TLS is dead on arrival — the dock is on `github.io` | the DM's machine must be reachable over **HTTPS with a real cert**; that alone chooses Cloudflare Tunnel over port-forwarding (plan §12.2) |
| `Content-Type: application/json` on a cross-origin POST **triggers a preflight** | the SSE stream looks perfect and rolls silently fail to submit | any local server the dock talks to needs a real `OPTIONS` handler, not just `Access-Control-Allow-Origin` (plan §12.3) |
| `http://127.0.0.1` from an HTTPS page drags in mixed-content exemptions **and** Chrome's Private Network Access preflight, and Safari is stricter than both | a loopback shortcut that works in one browser and not another | **no browser talks to loopback** — the DM's own dock uses the tunnel URL like everyone else; only the extension uses `127.0.0.1` (plan §12.2) |
| **Cloudflare Quick Tunnel (`*.trycloudflare.com`) does not deliver a streamed HTTP response body** — headers arrive, the body never does; isolated with a 10-line SSE server that streams fine on loopback and sends zero bytes through the tunnel | a live SSE stream through this tunnel shows nothing, ever, while an ordinary `GET` on the same server is fast | measured 2026-07-29 — **use WebSocket**, which is clean through the same tunnel (57ms avg end to end). SSE is loopback/debug only. Plan §12.11 |
| A browser applies **no CORS to a WebSocket upgrade** — no preflight, no `Allow-Origin`, no enforcement | any page anywhere can open a socket to a reachable server; the CORS work done for the POST path protects nothing here | the server must check the `Origin` header **itself** against the allow-list. No `Origin` at all = a non-browser client (curl, the bridge) and is allowed — the campaign code stays the single membership model. `ws.mjs`, unit-tested |
| `WORKER-ADMIN-API.md` in this repo is a **spec**, not the code | you will "fix" a document and deploy nothing | the deployed source is `~/tools/fh-worker/src/worker.js`, `wrangler deploy` from that directory |
| **Cloudflare KV meters `list()` separately from reads — 100,000 reads/day but ~1,000 `list()`/day on the free plan** | the dock's 3s feed poll is one `list()` each: **1,200/hour from a single open sheet**, so one player alone exhausts the day's allowance in 50 minutes and `/party` + `/feed` return `500` / code `1101` for everyone. This took production down on 2026-07-30 | **no `list()` on any path a page can call on a timer.** Prefix scans belong on DM-driven routes only; anything polled reads a single key. Plan §13.1 and §13.9 |
| `git branch -v` shows `gh-pages | Deployed <sha>` — the site's real deployed state | you reason about production from a plan note that says "not deployed" while the deploy happened weeks ago, and you misdate an incident by a whole architecture | check `gh-pages` and `git merge-base --is-ancestor <sha> main`, not the prose |
| **The FH-Mirror bench carries a PATCH the build does not** | `tools/dock-harness.html` points at `/stylesheets/` and `/javascripts/` — root-relative, correct on localhost where the server root *is* `site/`. GitHub Pages serves the bench under **`/FH-Mirror/`**, so every asset 404s and the dock never mounts: the page shows its intro note and nothing else, which reads like a broken build rather than a path problem | found 2026-08-06 by refreshing the bench with a straight copy of the build. **The refresh recipe is: build → copy into `~/tools/FH-Mirror` → rewrite the harness's asset paths to `./stylesheets/` and `./javascripts/` → commit → push.** Same family as the generated-artefact trap: a published copy whose source lives elsewhere drifts, and nothing in the copy says so. Verify by reading the live page back — `dockMonté`, zone count, and a computed `font-size`, not a `200` on the HTML |
| **A harness server outlives the worktree it serves** | removing a worktree does not kill the `python3 -m http.server` a session left on its port. The orphan keeps LISTENing with its cwd pointing at a deleted directory, so it answers **404 on everything**, and the replacement server cannot bind — it exits with "address already in use" and the browser blames the URL | found 2026-08-06, minutes after retiring eight worktrees: port 8127 was still held by a server whose cwd was `elastic-bell-0d62a0/site`. **After removing a worktree, check its port** — `lsof -nP -iTCP:<port> -sTCP:LISTEN`, and read the process's cwd (`lsof -a -p <pid> -d cwd`) before believing any 404. Same family as the orphan `cloudflared` below: a process that survives its directory lies about the state of the system |
| `cloudflared` does not die with `table-server.mjs` | an orphaned tunnel answers `1033` while the rendezvous record still says `live:true` for up to 15 min (`TABLE_TTL_SECONDS`, `worker.js:956`) | observed live 2026-07-30. Docks correctly show **OFF** — the design holds. But when killing a table server, kill its `cloudflared` too, or the next `ps` lies to you |
| **A glyph is judged on its 12px RASTER, never on its path** | the Tactical shield shipped as a proper heater shield whose flanks curved — the path describes a taper, and at 12px the corners rounded off and it rendered as a solid blob with no taper at all. The taper is the *entire* argument for the shield over the two rejected swords, so the glyph silently stopped being the thing it was chosen for | found 2026-08-03 building the roll vocabulary. **Straight edges and flat tops survive a 12px raster; curves at that size do not.** Draw each glyph into a 12×12 canvas and magnify the actual pixels — a screenshot of the page is downscaled and hides exactly this. Same measurement caught that `iconSvg`'s 1.65 stroke on a 24 grid is 0.8 device px in the tray, i.e. a hairline: source glyphs are filled, or stroked ≥3 |
| The vault's `Gpt in FH/FHPC-AboveVTT-DDB-Integration-Bible.md` recommends `/hit`/`/dmg`/`/save`/`/heal` **with a dice expression** as the production pattern — that makes AboveVTT roll its own dice (its own text confirms this) | posting one of these for a roll FHPC's engine already resolved shows the table a second, independently-rolled number, not the one Destiny/Chaos/Soulforge actually produced | a correction is appended at the top of that file (2026-07-29). Package 12b already avoids this (plain resolved text, plan §11.3) — the trap is only for a future "deepen" phase that follows the bible literally instead of "translate what you can, print what you cannot" |

---

## Source : `fh-phb/FHPC-V2-BRIEF.md` §6 — « Les pièges à ne surtout pas repayer »

Les 27 sont dans `ARCHITECT-HANDOFF.md` §3 (table ci-dessus). Ceux-ci mordent
la v2 **même en partant de zéro** :

| Piège | Ce qu'il impose |
|---|---|
| **Cloudflare KV : ~1 écriture/s par clé, et `list()` est compté à part (~1 000/jour)** | Une clé par événement + un curseur. **Aucun `list()` sur un chemin qu'une page appelle sur un timer** — ça a mis la prod à terre le 2026-07-30 |
| **La séquence EST l'horodatage** | Epoch ms 13 chiffres paddé + départageur aléatoire. Une clé compteur recrée la limite d'écriture |
| **Une page HTTPS ne peut pas fetch `http://`, et aucun navigateur ne parle au loopback** | Toute option d'auto-hébergement sans TLS est morte-née |
| **Cloudflare Quick Tunnel ne délivre pas de corps HTTP streamé** | WebSocket, pas SSE, dès qu'un tunnel est dans le chemin |
| **Un navigateur n'applique aucun CORS à un upgrade WebSocket** | Le serveur vérifie `Origin` lui-même |
| **`Content-Type: application/json` en POST cross-origin déclenche un preflight** | Tout serveur local a besoin d'un vrai handler `OPTIONS` |
| **Marquer livré avant de savoir si ça l'a été** | Réclamer optimiste si nécessaire, mais **dé-réclamer à l'échec**, et ne jamais laisser un succès non lié effacer un échec enregistré par quelqu'un d'autre. C'est une **famille** de bug, pas un cas |
| **Jamais de repli silencieux** | Service injoignable → on le dit et on refuse. Jamais rouler en local pendant que les joueurs croient que la table a vu |
| **Un glyphe se juge sur son raster 12px, jamais sur son chemin** | Bords droits et sommets plats survivent ; les courbes non |
| **Une note `// REWRITTEN` en milieu de ligne** a commenté quatre assertions et **rendu une suite verte alors que le garde ne tournait plus** | Les marques vont sur leur propre ligne. Quand un changement rend une assertion fausse : la réécrire à la nouvelle vérité et la marquer — jamais la relâcher, jamais la supprimer |

---

## Source : le dépôt `fhpc` lui-même — pièges payés ici, dans les tests

*Ajoutés à mesure. Chaque ligne est un défaut qui a été mesuré dans ce dépôt,
pas une précaution théorique.*

| Piège | Ce qui se passe | La règle |
|---|---|---|
| **Une suite qui écrit dans un artefact commité** (2026-08-08, lot 13) | `gen-srd-layer.test.mjs` appelait `generate()`, dont la destination était en dur, donc il écrasait `layers/*.layer.json` avant de comparer. La suite laissait l'arbre sale, et **l'exécution suivante héritait de la mutation** : deux passes d'affilée sans nettoyage ont rendu **307/1 puis 304/4**, sans qu'une ligne ait changé. Un test qui ment dans les deux sens | Un test qui vérifie une génération **génère dans un répertoire temporaire** et compare là. La destination d'un générateur est un **argument**, jamais une constante que seul le disque peut recevoir. Gardé par `tests/tree-immuable.test.mjs`, qui rejoue toute la suite entre deux relevés de l'arbre |
| **Une preuve qui cesse de prouver sans que personne n'y touche** (2026-08-08, lots 8 → 13) | Les refus de la dérivation se prouvaient tout seuls tant que la couche SRD était pauvre : il suffisait de la monter. La source s'est enrichie deux fois (lot 8, puis lot 11 + régénération), et ces preuves-là se sont évaporées **en restant vertes** | Un test qui montre un refus s'appuie sur une **privation délibérée** — une couche de scénario qui ampute un record — jamais sur une pénurie de circonstance. Voir `ampute()` dans `tests/build-harness.mjs` |
| **Un garde de vocabulaire calé sur le mot du cahier des charges** (2026-08-08, trois fois) | `arcane?` ne voyait pas `arcana`, l'arpenteur à plat ne voyait pas les sous-répertoires, et `\bdestiny\b` ne voyait **aucune forme composée** — `spendDestiny`, `rollChaos`, `FH_DESTINY`, `destinyDie`. Trois retours de la même famille sur la loi la plus haute du chantier | Un garde se teste sur **les formes que le code emploie**, et il s'attaque **dans les deux sens** : la violation doit rougir, le code légitime doit rester vert. Un garde qui crie au loup se fait désactiver, et c'est la garantie entière qui part avec lui |
| **Une suite verte sur des artefacts périmés** (2026-08-08) | `fh-srd` a bougé pendant un lot ; les couches commitées ne venaient plus de la source, et rien ne rougissait | Vérifier qu'un build a **réellement réussi** avant de croire son silence. `gen-srd-layer.test.mjs` compare désormais la sortie fraîche aux fichiers commités — il peut rougir sur une couche périmée, ce que l'ancienne version ne pouvait plus |
| **Une prose SRD recopiée dans une couche pour y changer trois mots** (2026-08-08, lot 17) | La description du Hoddon disait « As a Gnome » et celle de l'Elfe citait « Perception ». La corriger en recopiant le blob SRD marche **le premier jour** : le jour où `fh-srd` retouche ce texte, la copie ne bouge pas, plus rien ne la relie à sa source, et **personne ne le voit** — c'est la famille de l'« artefact généré patché dans sa copie publiée », appliquée à du texte | **On ne recopie pas la prose, on DÉCLARE la substitution et on la recalcule.** La source déclare le motif cherché, le texte posé et **pourquoi** ; le générateur lit le texte SRD courant et applique. Une substitution qui ne trouve pas sa cible **jette en la nommant** — c'est elle qui transforme la dérive en alarme. Et un second filet relit le RÉSULTAT (`mustNotContain`), parce qu'une **phrase neuve** du SRD laisserait tous les motifs satisfaits et redirait quand même le mot banni |
| **Un `required` seul dans un `anyOf`, en mode strict ajv** (2026-08-08, lot 17) | `{"required": ["changes"]}` fait **planter la compilation du schéma** (`strictRequired`), pas échouer un cas : trois suites tombent d'un coup avec une trace ajv et zéro test exécuté. Un `$comment_xxx` inventé fait la même chose (`unknown keyword`) | Dans une branche de `anyOf`/`oneOf`, **redéclarer la propriété** dans la même branche (`"properties": {"changes": true}` suffit). Et **aucun mot-clef inventé** dans un schéma, `$comment` compris : c'est `$comment` ou rien |
| **Les unités de conteneur ne sont pas rebasées par `zoom` sous WebKit** (2026-09-02, lot 121) | `100cqw` rend des **pixels PEINTS** là où le dépôt compte en **blg** (NORMES §0 bis). Sur l'écran *Ability boosts*, l'iPhone (zoom 1) lisait 352 → `(352−40)/6 = 52` → six caracs sur une ligne ; l'iPad (zoom 1,83) lisait **644** → `(644−40)/6 = 101` → `min(87 ; 101) = 87`, six cases demandant **562 blg dans 352** : rangée centrée, `STR` et `CHA` coupées aux deux bords. Même famille exactement que « `zoom` ne rebase pas `vw`/`vh` » (lot 85, seconde passe). Trois sites portaient le défaut ; Eric n'en a vu qu'un, parce qu'il faut être sur l'écran précis | **mesuré par CONTRASTE, pas déduit d'un seul relevé** : une sonde dans Chromium, sous la même app zoomée à 1,829, avec la formule réelle et un conteneur de 352, rend **51,996** — la valeur en unités de dessin. **Chromium rebase `cqw`, Safari non** : les deux moteurs étant en désaccord, ⛔ **aucune formule écrite en unité de conteneur ne peut être juste sur les deux** — ce n'est pas un réglage à trouver, c'est une unité à retirer. ⭐ La sortie n'est PAS le retour à `%` (un pourcentage se résout chez celui qui l'utilise, la panne n°3) : depuis la règle sacrée du 31/08 le panneau vaut toujours 375 blg, donc la colonne est une **constante en blg** et la cote se **déduit** (§1 ter) — `--colonne-ecran` / `--colonne-dalle`, posées sur `.stage`. ⚠️ Le comportement de WebKit est **déduit des captures d'Eric**, jamais mesuré sur son iPad : le `WKWebView` de macOS diverge de WebKit d'iPadOS (logbook du 31/08), donc un banc local ne prouverait rien. Gardé par `tests/unite-de-conteneur.test.mjs` |
| **Une query `?v=` dans un import de `src/`** (2026-08-15, lot 75) | Pour Node, `registry.mjs` et `registry.mjs?v=1` sont **deux modules** : le kernel (`blocks = new Map()`) s'est dédoublé — le bloc s'enregistrait sur une instance, `dispatch` interrogeait l'autre. « missing block(s): layers », **35 tests rouges d'un coup, serveur MCP sur le tuyau compris**. Et `new URL(relatif, base)` **jette la query de la base** : `import.meta.url` versionné ne se transmet pas tout seul à un `fetch` | Les versions vivent dans `ui/` (imports en toutes lettres) et dans l'**import map générée** d'`index.html` pour les modules `src/` — les sources `src/` restent **vierges de query**, Node ne voit qu'une instance de chaque. Un chargement d'exécution met la version **dans le chemin** (`versionQuery(import.meta.url)`). Gardé par `tests/versions-graphe.test.mjs` (test 4 : virginité ; test 5 : map exacte) |
| **Un garde qui épelle une IMPLÉMENTATION plutôt qu'un INVARIANT** (2026-09-02, lot 124) | Le garde 21 de `shell-wiring` recopiait la ligne fautive (`parcoursNext… action.racine !== parcoursRacineCourante()…`). Il ne protégeait donc plus la règle, il **protégeait le bug** : il serait resté vert pendant que le `Next` d'Identity ne menait nulle part, et il aurait **rougi sur sa réparation**, avec l'air d'avoir raison. Le défaut a vécu deux mois sous sa garde | Un garde décrit ce qui doit **rester vrai**, jamais **comment c'est écrit**. Quand un correctif fait rougir un garde, la première question est *« lequel des deux a tort ? »* — et la réponse est le garde chaque fois qu'il cite du code au lieu d'une promesse. Le réécrire à la nouvelle vérité, jamais le désarmer |
| **Un test qui ne couvre qu'une MOITIÉ d'une alternative** (2026-09-02, lot 124) | Le même garde tenait *« un item emboîté ne change pas d'étape »* et **personne** ne tenait *« un item non emboîté AVANCE »*. C'est la moitié non gardée qui a laissé vivre le bouton mort. Même famille que le quatrième état d'Identity (`NORMES §6` : *« un `else if` sans `else` ne prévient jamais qu'il ne couvre pas tout »*) et que « une absence n'est jamais une réponse » | Une alternative se garde **des deux côtés**, dans le même test : le cas qui doit refuser ET le cas qui doit passer. Un garde qui n'éprouve qu'une branche laisse l'autre libre de mentir — et c'est toujours l'autre qui casse |
| **Le volet de prévisualisation sert un AUTRE dépôt que celui qu'on nomme** (2026-09-04, deux sièges le même soir) | `preview_start` rend un `serverId` et un port parfaitement crédibles, et le contenu servi vient d'ailleurs. **Mesuré dans les deux sens** : l'architecte demande le builder et reçoit le port **8942**, un `python3 -m http.server` **orphelin** enraciné sur `~/tools/fh-worktrees/142-carte-tiree`, en **v474** alors que le disque est en **v527** — tout le premier relevé des rangées de contrôles était faux, et il avait l'air cohérent. Le siège FH WEB demande `fh-site-b` (déclaré dans `fh-phb/.claude/launch.json`) et reçoit **`fhpc-142`, enraciné sur `~/tools/fhpc`** — deux fois de suite ; il a cru mesurer son dépôt et mesurait l'autre. ⛔ Le port ne dit rien de la racine, et le nom de configuration non plus | **Comparer l'empreinte du contenu SERVI à celle du disque avant de croire un seul chiffre** — ici `curl -s localhost:<port>/ui/builder/index.html \| grep -o 'shell.css?v=[0-9]*'` contre le même `grep` sur le fichier. En cas d'écart, trouver la vraie racine : `lsof -t -nP -iTCP:<port> -sTCP:LISTEN` puis `lsof -a -p <pid> -d cwd`. ⭐ Même famille que « un serveur de banc survit au worktree qu'il sert » et que « un port n'est pas une preuve d'origine » : **un processus qui survit à son répertoire ment sur l'état du système**, et un résolveur qui choisit un port ment sur le dépôt. ⚠️ Et tuer son propre serveur de mesure en partant — sinon c'est lui l'orphelin de la prochaine session. 🔴 **ET LE NUMÉRO DE VERSION NE SUFFIT PAS — mesuré le 2026-09-06 à 02:00.** Le volet a rendu un banc nommé `fhpc-162`, **enraciné sur `fh-worktrees/162-abilities-b1`** alors que l'architecte demandait le sien ; et l'empreinte prescrite ci-dessus **a dit OUI** : servi `v579`, disque `v579`. ⛔ Les deux dépôts portaient le même numéro à la même minute, donc **une empreinte de VERSION valide un banc qui n'est pas le vôtre**. Ce qui tranche est l'empreinte de **CONTENU** : `curl -s <url>/<fichier> | md5` contre `md5 -q <fichier>`, **sur chacun des fichiers dont dépend la mesure** — ici `shell.css`, `shell.mjs`, `tokens.css`, `index.html` et l'étape mesurée, cinq md5 identiques, et c'est seulement à ce prix que le relevé qui a suivi vaut quelque chose. ⭐ Même famille que « une bijection fausse est cohérente » : **un contrôle qui répond OUI trop facilement est pire qu'aucun contrôle**, parce qu'il achète la confiance sans la mériter. ⚠️ Et un banc qui survit à son siège est orphelin dès la passation, pas le lendemain : le port 8942 servait encore `v579` **à 02:02:57**, juste, ce soir-là seulement — il mentira le jour où sa branche divergera, sans rien annoncer |
| **Le checkout principal `~/tools/fhpc` n'est PAS toujours sur `main`** (2026-09-06, signalé par le siège Bible, qui l'a payé) | Un siège ouvre `~/tools/fhpc`, écrit, commite — et fait **avancer la branche d'un autre siège** sans jamais voir d'erreur : `git status` est propre, la suite est verte, le commit part. **Mesuré au reflog** : le checkout principal a changé de branche **huit fois en deux jours** (`main` → `158-cancel-partout` → `main` → `159-vocabulaire-porte` → `main` → `160-prompt-mene-a-la-bible` → `main` → `161-garde-creux` → `main`), et l'architecte est l'un des coupables. ⛔ Le chemin ne dit rien de la branche, et **119 worktrees** existent précisément pour que personne n'ait besoin de basculer celui-là | **Lire la branche AVANT d'écrire, jamais après** : `git rev-parse --abbrev-ref HEAD` puis `git status -sb` dans le même souffle, et le dire dans le rapport. Un lot de code va dans **son** worktree (`~/tools/fh-worktrees/<numéro>-<nom>`), jamais dans le checkout principal ; celui-ci se garde pour `main`. ⭐ Même famille que « le volet sert un AUTRE dépôt que celui qu'on nomme » et que « un port n'est pas une preuve d'origine » : **un contenant familier ment sur son contenu**, et seule une mesure de l'identité — pas du chemin — l'attrape. ⚠️ Et si tu as basculé : reviens sur `main` en partant, sinon c'est le siège suivant qui paie. 🔴 **ET ÇA MORD DANS L'AUTRE SENS, mesuré trois minutes après l'écriture de cette ligne** : l'architecte commite à 01:33, et à 01:36 le reflog enregistre un `pull --rebase origin main` **qu'il n'a pas lancé** — un autre siège a rejoué son commit non poussé sur un `main` plus récent. Sans dégât cette fois (1 fichier, arbre propre), mais **l'historique d'un siège a bougé sous ses pieds sans un mot**. Donc : ⛔ **jamais de `commit --amend`, de `rebase -i` ni de `reset` dans le checkout principal** — on n'y réécrit pas une histoire que quelqu'un d'autre est peut-être en train de rejouer. Et **relire `git log --oneline -3` juste avant de pousser**, jamais se fier au relevé d'il y a dix minutes |
| **`glisser(de, null)` n'est pas un lâcher dans le vide — c'est un TAP** (2026-09-06, siège Abilities) | Sans cible, aucun `pointermove` n'est émis : le **seuil de 6 px n'est jamais franchi**, et le geste retombe dans la branche du **tap**. Les deux appellent `reprendre`, donc le test « lâcher dans le vide » passait — **en mesurant autre chose**. ⭐ Ce n'est pas l'assertion qui l'a attrapé, c'est **le témoin contraire** : le témoin B1, qui devait rester à ZÉRO, a refusé de rougir | **Un garde sans témoin contraire ne prouve rien.** Un test qui vérifie qu'un geste FAIT quelque chose se double d'un témoin qui vérifie qu'un autre geste ne le fait PAS — sinon les deux chemins peuvent se confondre sans que rien ne le dise. ⚠️ Et un geste de pointeur se prouve **par les événements qu'il émet**, jamais par la fonction qu'on appelle : `glisser(a, b)` et `glisser(a, null)` ne sont pas deux cas du même geste, ce sont **deux gestes** |
| **Une règle de cote ou de teinte écrite AVANT le bloc de sa famille perd en silence** (2026-09-06, payé **deux fois la même nuit**) | À spécificité égale, c'est l'ORDRE qui départage : la dernière écrite gagne. Une règle posée trop haut dans la feuille est donc **écrasée par le défaut de sa propre famille**, sans erreur, sans avertissement, et **avec l'air d'exister** — elle est bien dans le fichier, elle ne s'applique simplement jamais. ⛔ Le siège qui l'a payé avait **lu §7.8, qui documente exactement ça, le matin même** | **Une règle qui surcharge un défaut se pose APRÈS le bloc qu'elle surcharge.** Et quand une déclaration « n'a pas d'effet », la première question n'est pas *« est-elle juste ? »* mais *« qui la gagne ? »* — `getComputedStyle` sur l'élément rendu répond en une ligne. ⭐ Même famille que « `:not()` porte la spécificité de son argument » (03/09) : **la cascade se mesure, elle ne se raisonne pas** |
| **Un agent annonce une livraison sur le disque qu'il ne peut pas faire** (2026-09-06) | ChatGPT a annoncé avoir rangé une image dans le vault. **Elle n'y était pas** — il n'a aucun accès au disque. L'annonce était sincère et fausse, et rien dans sa forme ne la distinguait d'une vraie | **Une livraison se vérifie AU DISQUE** (`ls`, `md5`, la taille), **jamais sur la parole de qui l'annonce** — et c'est vrai de tout siège, humain ou non. ⭐ Même famille que « un port n'est pas une preuve d'origine » et que « une empreinte de VERSION valide un banc qui n'est pas le vôtre » : **ce qui affirme n'est jamais ce qui prouve** |
| **Un siège armé par TÂCHE PLANIFIÉE est INJOIGNABLE — avant, pendant ET après** (2026-09-06, mesuré par Archi 32 à 20:57) | Archi 31 a armé trois sièges à 5 h, 6 h et 7 h sans pré-approuver leurs outils. Les trois ont démarré, lu leur mandat, puis **gelé sur leur première commande de mesure** (`git status`, `ls`) : **trois sièges perdus, zéro ligne produite** — et l'outil l'avait annoncé à la création. 🔴 **ET LA SECONDE MOITIÉ, MESURÉE CE SOIR** : le siège suivant a voulu les prévenir que leur sujet avait été livré sans eux, et `send_message` a **refusé les trois**, avec le même motif — *« session is unattended (a scheduled-task run or dispatched session) »*. ⛔ Ils apparaissent pourtant `isRunning: true` dans `list_sessions`, six heures après leur dernière activité : **la liste les montre vivants, la messagerie les déclare hors d'atteinte.** Un siège planifié ne peut donc être ni corrigé en route, ni averti ensuite, ni débriefé | **Ce qu'un mandat planifié ne porte pas au départ est perdu pour de bon** — pré-approuver ses outils, et ne l'armer que quand la consigne est complète (Archi 31 en a lancé un avec une cote périmée et un autre sans la ligne éditoriale d'Eric, faute de pouvoir les rattraper). ⭐ **Et ce qu'on ne peut pas leur demander, on le MESURE à leur place** : à 20:58 aucun banc orphelin n'écoutait (seuls `Scripts/dash` sur 9876 et les ports internes de Claude), et **un seul** des 122 worktrees était sale — `2-normes-visuelles`, un `banc-mesure.html` non suivi datant d'un commit du 26/08, donc étranger à ces trois-là. ⚠️ Les quatre tâches sont `enabled: false` et one-time, **déjà tirées** : rien ne se rallumera. ⭐ Même famille que « un sous-agent lancé par `Agent` ne peut pas être joint en cours de route », en pire : celui-là, on le vérifie au moins **à l'arrivée** |
| **UN CHIFFRE QUI DÉPEND D'UN MOT AMBIGU NE PEUT PAS SERVIR DE TÉMOIN** (2026-09-06, attrapé en traversant une passation) | La passation d'Archi 32 annonçait *« 33 mentions du site dans 13 pages sur 44 »* comme étant **le relevé du siège Bible**. Ce n'en était pas un : c'était l'**estimation d'Archi 31 AVANT que Bible ne mesure**. Le relevé réel est **67 mentions / 24 pages → 46 faux positifs · 20 restent · 1 part**. ⛔ Et **aucun des deux n'a tort** : le filtre d'Archi 31 attrape le mot juste, celui de Bible attrape le mot **ambigu** (il a inclus « chapitre », « le livre », « du site »). 🔴 Le chiffre faux a franchi une passation, est entré dans le vault, et se serait cité tout seul | ⭐ **Tant qu'un mot porte deux sens, tout comptage qui l'emploie rend un nombre différent selon qui le lance — donc il ne prouve rien.** Un témoin se nomme AVANT de mesurer, et un chiffre relayé porte **qui l'a mesuré et avec quel filtre**, jamais le nombre seul. ⚠️ Corollaire mesuré la même minute : le diff `main..161-corpus-une-source` rendait **4328** suppressions à 20:58 et **4329** à 21:01 — parce que `main` avait gagné une ligne entre les deux. **Un nombre mesuré a une HEURE, même quand il ne parle pas du temps** |
| **UN COMPTE DE COMMITS NE DIT PAS CE QU'UNE BRANCHE PORTE** (2026-09-06, mesuré des deux côtés) | `git log main..161-corpus-une-source` affiche **9 commits « absents de `main` »** — et le contenu de ces neuf commits est **intégralement dans `main`**. Une lecture du compte conclut « neuf commits à récupérer » ; la vérité est l'inverse exact. 📏 Le **diff à deux points** tranche en une ligne : `git diff --shortstat main..<branche>` rend **459 ajouts contre 4 329 suppressions** — ce sont des suppressions de ce que `main` a en plus, donc **c'est la branche qui est en retard**. Idem sur `163-titre-375` (1 commit, 346 / 3 146) et `164-destiny-signe` (1 commit, 328 / 3 041) : trois **doublons périmés**, rien à récupérer | **Un commit et un contenu sont deux choses.** Avant de croire qu'une branche porte du travail, lire `git diff --shortstat main..<branche>` : beaucoup plus de **suppressions** que d'ajouts = la branche est **derrière**, pas devant. ⭐ Même famille que « 0 conflits réels peut mentir alors que l'outil compte juste » : **un compteur compte ce qu'il compte, pas ce qu'on lui fait dire**. ⛔ Et sept autres branches non fusionnées (`110-score-et-des`, `115-accolade-gate`, `116-destiny-colonne`, `117-dressing-375`, `118-rampe-obsolete`, `123-species`, `144-rebase`) datent des Archis 29/30 et **n'ont été vérifiées par personne** : ne rien supprimer sur la parole d'un siège |
| **`preview_start` RÉASSIGNE LE PORT EN SILENCE — le nom ne dit pas la racine, et le port ne dit pas le nom** (2026-09-06, relevé par le siège Abilities) | `fhpc/.claude/launch.json` demande **8942** ; le port était **pris**, et `preview_start` a assigné **54624** (autoPort) **sans que le nom du banc change**. Donc à 02:00, quand l'architecte croyait mesurer `fhpc-162` sur 8942 : il y avait **au moins deux bancs vivants sur deux dépôts différents**, et le nom `fhpc-162` désignait celui d'Abilities — dont le port **n'était pas celui du fichier de configuration**. ⛔ Le relevé relayé au standard (*« 8942, pid 20896, enraciné sur `162-abilities-b1` »*) et celui d'Abilities (*« le mien était sur 54624 »*) **se contredisent**, et personne ne peut plus les départager : les deux bancs sont morts | **Trois choses distinctes, et aucune ne prouve les deux autres : le NOM, le PORT, la RACINE.** ⭐ Seule l'empreinte de **CONTENU** tranche, et le meilleur témoin est un octet qui **n'existe que d'un côté** : `assets/abilities/tapis-4x4.webp` = **10 670 o**, absent avant **v584** — il ne dit pas seulement *quel dépôt*, il dit *quelle époque*. ⚠️ Un témoin qui identifie sans **dater** laisse passer un banc du bon dépôt et du mauvais jour |
| **UN MATCH N'EST PAS UNE PREUVE TANT QU'ON N'A PAS LU SON CONTEXTE** (2026-09-06, siège Abilities) | `grep -i "point cost"` sur `layers/srd-5.2.1-en.layer.json` **répond 1**. Qui s'arrête là conclut que le barème du **Point Buy** existe dans la couche. ⛔ Le match est `« Spell Slot Level \| Sorcery Point Cost \| Min. Sorcerer Level »` — **la table des points de sorcellerie du Sorcerer**, un homonyme. 📏 Le vrai compte : `point buy` **0** · `point_buy` **0** · `pointbuy` **0** · `point_cost` **0**. Le barème **n'existe nulle part** dans le dépôt, ce qui est déjà la raison pour laquelle la tuile n'est pas offerte | **On lit le contexte d'un match avant d'en tirer une conclusion**, `grep -n` puis la ligne entière — et sur une recherche qui doit rendre zéro, on essaie **plusieurs orthographes** avant de croire le zéro. ⭐ Même famille que « un total juste ne dit rien du contenu » et que « une bijection fausse est cohérente » : **un contrôle qui répond OUI trop facilement est pire qu'aucun contrôle** |
